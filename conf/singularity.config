// Singularity profile configuration
profiles {
  singularity {
    process.executor = 'local'
    singularity.enabled = true
    // use library:// or local image reference
    process.container = 'docker://ginflow:0.1.0'
  }
}