// params.config
// Central configuration for pipeline parameters (loaded via root nextflow.config)
// Defaults are defined in base.config; JSON presets are provided in presets/ and loaded via -params-file

// Example usage:
// nextflow run . -profile docker -params-file presets/nf-params.json

// Add any extra parameter overrides here if needed