includeConfig "${projectDir}/conf/modules/gpu_runtime.resources.config"
includeConfig "${projectDir}/conf/modules/build_cuvs_index.resources.config"

// BUILD_CUVS_INDEX is GPU-only; its accelerator and runtime options come from
// the production resource configuration.
